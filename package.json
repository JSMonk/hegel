{
  "name": "static-type-checker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:check && npm run build:clean && npm run build:start",
    "build:check": "flow",
    "build:start": "flow-remove-types src/ -d build/",
    "build:clean": "rm -rf build && mkdir build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/parser": "^7.1.3"
  },
  "devDependencies": {
    "flow-bin": "^0.83.0",
    "jest": "^23.6.0"
  }
}
